Create a professional README.md file for an Excel project named "Date Functions in Excel".

The Excel sheet demonstrates the following functions:
1. TODAY() – Returns the current date.
2. NOW() – Returns the current date and time.
3. WEEKDAY() – Returns the weekday number.
4. DAY() – Extracts the day from a date.
5. MONTH() – Extracts the month from a date.
6. YEAR() – Extracts the year from a date.
7. DATE() – Creates a date from year, month, and day values.

For each function, include:
- Purpose
- Syntax
- Example Formula
- Example Output
- Short explanation

Also include:
- Project Overview
- Features
- Functions Used Table
- Conclusion

Format the README using proper Markdown headings, tables, and code blocks. Keep the language simple and suitable for students learning Excel.
